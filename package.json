{
  "name": "skill-widget",
  "version": "0.1.0",
  "private": true,
  "description": "Always-on-top macOS edge widget listing Claude Code skills and commands, with search and copy-to-clipboard prompt preparation.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test:unit": "vitest run tests/unit",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:e2e",
    "package": "electron-packager . SkillWidget --platform=darwin --out=dist --overwrite"
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "@playwright/test": "^1.62.1",
    "electron": "^43.4.0",
    "playwright": "^1.62.1",
    "vitest": "^4.1.10"
  }
}
