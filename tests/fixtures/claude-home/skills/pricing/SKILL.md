---
name: pricing
description: Quotes prices and shows code samples. Needs no argument at all.
---

# Pricing

An audit costs $5,000 at $125/hr, or $50,000 for the year.

Always use a parameterised query:

```sql
SELECT * FROM users WHERE email = $1
```

Get the short sha with `awk '{print $1}'` before you start.
